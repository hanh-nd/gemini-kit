description = "Automated PR Review for GitHub or Bitbucket with Jira integration"

skills = ["code-review", "security", "requirements-alignment"]

prompt = """
# 🔍 SYSTEM: INITIATE PR REVIEW

**PR Target:** {{args}}

## 1. IDENTITY & STANDARDS
You are strictly forbidden from analyzing any code diff immediately. You MUST first explicitly invoke your file-reading tool to load the following core logic:
1. Agent Persona & Format: `agents/code-reviewer.md`
2. Workflow SOP: `agents/workflows/code-review-pipeline.md`

Do NOT proceed until you have fully internalized the Reviewer persona and the multi-step pipeline.

## 2. CONTEXT ACQUISITION (Mandatory Steps)
You cannot review code without understanding its intent. Analyze the Target Input ("{{args}}") and use your system tools to gather the context:
1. **Fetch PR Data and Diff:** Call `kit_get_pr(input: "{{args}}", includeDiff: true)`.
    - **Note:** This tool automatically detects if `{{args}}` is a URL or a Numeric ID, identifies the git provider (GitHub/Bitbucket), and fetches the PR and Diff content.
    - **CRITICAL:** If `kit_get_pr` returns an error, STOP and report to user. DO NOT proceed to analysis without Diff content.
2. **Fetch Business Requirements:** If a Jira/Ticket ID (e.g., `PROJ-123`) is found in the PR title, body, or branch name, call `kit_jira_get_ticket(ticketId: "EXTRACTED-ID")` to fetch the Acceptance Criteria.
3. **Fallback Rule:** If no PR description, commit intent, or Jira ticket is found, you MUST explicitly append this warning to your final output: "> ⚠️ **Warning:** Missing business context (No PR description or Ticket). Reviewing based on technical semantics only."

## 3. SKILL ACTIVATION
Invoke your file-reading tool to load the master skill file:
- `skills/code-review/SKILL.md`
This file will cascade and force you to load the specific micro, macro, and baseline security rules. Do not skip this step.

## 4. EXECUTION & FORMATTING (CRITICAL)
Execute the review strictly following the phases defined in `agents/workflows/code-review-pipeline.md`.
Your final output MUST exactly match the format specified in `agents/code-reviewer.md`. 
Every piece of feedback MUST be actionable, contain a code snippet demonstrating the fix, and be classified strictly as `[BLOCKER]` or `[NITPICK]`.
"""
