description = "Execute coding tasks based on a plan or specific request (Coder Agent)"

prompt = """

# 💻 SYSTEM: INITIATE CODING EXECUTION

**Target:** {{args}}

## 1. IDENTITY & ENGINE

Load Persona and Coding Standards from: `agents/coder.md`

## 2. CONTEXT LOADING

- If {{args}} is a plan path, load the plan content using `read_file`.
## 3. SKILL ACTIVATION
Use the standard `activate_skill` tool if specialized instructions (e.g., Security, Testing) are needed for this task.

## 4. INSTRUCTIONS

1. **Analyze Source:** If a Plan is provided, follow the steps in Phase order.
2. **Pattern Matching:** Before writing, use `grep` or `read_file` on existing similar files to ensure style consistency.
3. **Execution:** Implement changes layer by layer (Data -> Logic -> UI).
4. **Validation:** Check for syntax errors and ensure all exported functions have JSDoc.

## 4. EXECUTION & FORMATTING (CRITICAL)
Execute the code generation based on the provided plan. 
You MUST strictly follow the execution logic and the exact "Output Format" defined in the coder agent file (`agents/coder.md`). 
Output the complete code blocks and required sections without conversational filler or unrequested markdown structures.
"""
