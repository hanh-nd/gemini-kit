description = "Create implementation plans using Agent-led Phase Routing"

prompt = """
# 🚀 SYSTEM: INITIATE PLANNING SEQUENCE

**Target Requirement & Provided Context:** {{args}}

## 1. IDENTITY & STANDARDS
You are strictly forbidden from generating a plan immediately. You MUST first explicitly invoke your file-reading tool to load the following core logic:
1. Agent Persona & Format: `agents/planner.md`
2. Workflow SOP: `agents/workflows/planning-pipeline.md`

Do NOT proceed until you have fully internalized the Planner persona and the multi-step pipeline.

## 2. EXECUTION & FORMATTING (CRITICAL)
Execute the planning logic strictly following the phases defined in `agents/workflows/planning-pipeline.md`.
Your final output MUST exactly match the state formats specified in `agents/planner.md`.
"""
