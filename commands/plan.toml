description = "Create implementation plans using Agent-led Skill Routing"

prompt = """
# 🚀 SYSTEM: INITIATE PLANNING

**Objective:** {{args}}

## 1. IDENTITY & WORKFLOW
Load Persona and Protocol from: `agents/planner.md`

## 2. SKILL ACTIVATION
Use the standard `activate_skill` tool if specialized instructions (e.g., Security, Testing) are needed for this task.

## 3. INSTRUCTIONS
1. **Analyze:** Parse the user input "{{args}}".
2. **Discover:** Use CLI tools to inspect the local codebase.
3. **Execute:** Generate the plan following the Agent's output states.

## 4. EXECUTION & FORMATTING (CRITICAL)
Execute the planning logic. 
You MUST strictly follow the execution logic and the exact "Output Format" defined in `agents/planner.md`. 
Do NOT invent your own headings, sections, or conversational filler. Output only the requested structured content.
"""
