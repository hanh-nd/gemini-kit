description = "High-level trade-off analysis and strategic architectural decisions"

# Architecture: Command (Context) -> Agent (Thinker) -> Skills (Specialized Knowledge)
prompt = """
# 💡 SYSTEM: INITIATE BRAINSTORMING SESSION

**Topic:** {{args}}

## 1. IDENTITY & STRATEGY
Load Persona and Strategic Framework from: `agents/brainstormer.md`

## 2. DYNAMIC CONTEXT SCAN
- **Project Pulse:** Scan the root directory and `package.json` to understand the current technical ecosystem (e.g., Node.js, Fedora environment, existing tools).
- **Previous Context:** Check `.geminit-kit/tmp/` for any existing scout reports or plans that might influence this decision.

## 3. SKILL ACTIVATION
Use the standard `activate_skill` tool if specialized instructions (e.g., Security, Testing) are needed for this task.

## 4. EXECUTION & FORMATTING (CRITICAL)
Execute the brainstorming session. 
You MUST strictly follow the execution logic and the exact "Output Format" defined in `agents/brainstormer.md`. 
Do NOT invent your own headings, sections, or conversational filler. Output only the requested structured content.
"""
