description = "High-level strategic architectural decisions and trade-off analysis"

skills = []

prompt = """
# 💡 SYSTEM: INITIATE BRAINSTORMING SEQUENCE

**Target Topic / Requirement:** {{args}}

## 1. IDENTITY & STANDARDS
You are strictly forbidden from generating any solutions immediately. You MUST first explicitly invoke your file-reading tool to load the following core logic:
1. Agent Persona & Format: `agents/brainstormer.md`
2. Workflow SOP: `agents/workflows/brainstorm-pipeline.md`

Do NOT proceed until you have fully internalized the Brainstormer persona, the strict pushback rules, and the multi-step pipeline.

## 2. EXECUTION & FORMATTING (CRITICAL)
Execute the brainstorming session strictly following the interactive phases defined in `agents/workflows/brainstorm-pipeline.md`.
Your final output MUST exactly match the state formats specified in `agents/brainstormer.md`.
Remember to HALT and ask for user input at every designated checkpoint. Do NOT batch multiple phases into a single response.
"""
