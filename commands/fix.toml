description = "Execute targeted bug fixes based on a debug report or manual prompt"

prompt = """
# 🔧 SYSTEM: INITIATE TARGETED FIX

**Input Context:** {{args}}

## 1. AGENT & SPECIALIZATION
Load Persona and Fix Logic from: `agents/coder.md`

## 2. CONTEXT INGESTION
- **If Input is a Debug Report:** If {{args}} points to `tmp/debugs/debug-*.md`, you MUST use `read_file` to ingest the Root Cause and Fix Plan.
- **If Input is a Manual Prompt:** Treat "{{args}}" as the direct fix requirement.

## 3. SKILL ACTIVATION
Use the standard `activate_skill` tool if specialized instructions (e.g., Security, Testing) are needed for this task.

## 4. EXECUTION GUIDELINES
1. **Verify Location:** Confirm the exact file and line number to be modified.
2. **Minimal Surgery:** Change ONLY what is necessary to resolve the bug. Do not perform unrelated refactoring.
3. **Safety First:** If the fix involves sensitive logic, apply @SEC_AUDIT standards.

## 5. EXECUTION & FORMATTING (CRITICAL)
Implement the targeted bug fix. 
You MUST strictly follow the execution logic and the exact "Output Format" defined in `agents/code-fixer.md`. 
Provide ONLY the requested sections (Fix Context, The Fix, Verification Steps, Regression Check) without adding generic conversational text or unnecessary refactoring.
"""
