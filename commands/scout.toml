description = "Explore and map the codebase structure (Scout Agent)"

prompt = """
# 🔍 SYSTEM: INITIATE CODEBASE SCOUTING

**Mission:** Explore and analyze the codebase for: {{args}}

## 1. IDENTITY & PROTOCOL
Load Persona and Investigation Logic from: `agents/scout.md`

## 2. SKILL ACTIVATION (CRITICAL)
Before executing the core task, you MUST perform the following steps in exact order:
1. Call the `kit_get_skill_list` tool to retrieve all available skills and their descriptions.
2. Analyze the user's request and the target files to determine which skills are strictly necessary.
3. If you determine that you need a skill, load the content of the chosen skills from path of the skill.
4. Do NOT proceed with the main task until the chosen skills are fully loaded into your context.

## 3. INSTRUCTIONS
1. **Identify Project Name:** Read `package.json`, `go.mod`, or the root folder name.
2. **Execute Scouting:**
    - Use `ls`, `grep`, `find`, or `tree` to map the landscape.
    - Identify tech stack and core architectural patterns.
3. **Analyze:** Organize findings into the Categorized Landscape.
4. **Identify Integration Points:** Trace how files/services interact.

## 4. EXECUTION & FORMATTING (CRITICAL)
Execute the investigation and codebase mapping. 
You MUST strictly follow the execution logic and the exact "Output Format" defined in `agents/scout.md`. 
Output ONLY the structured map and findings. Do NOT add unnecessary conversational preambles or attempt to fix the code during the scout phase.
"""
